import { config } from '@/lib_D/useApi';

export const fnArray = config.formula.array;
export const fnFilter = config.formula.filter;
export const fnTransform = config.formula.transform;
export const fnName = config.formula.name;

export const ApiComparisonSymbol = config.operator.comparisonSymbol;
